# Parity merge gate

Do not merge until the full Docker benchmark completes and the target dark/high-contrast cases are visually inspected against the saved MATLAB results and kernels. Unit tests alone are not sufficient for this numerical port.
