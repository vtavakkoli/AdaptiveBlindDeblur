# Targeted regression cases

The parity report must include and retain native-resolution outputs for `26.png`, `7_patch_use.png`, `IMG_0650_small_patch.png`, `IMG_0664_small_patch.png`, `IMG_4561.JPG`, `blurry_7.png`, `postcard.png`, and `real_leaffiltered.png` because they exercise the dark-channel, saturation, long-PSF, and gradient-only failure modes found during the audit.
